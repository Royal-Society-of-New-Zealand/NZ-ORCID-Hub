version: '2'
services:
  app:
    image: nad2000/orcidhub-app
    domainname: orcidhub.org.nz
    hostname: app
    volumes:
      - ./app:/var/www/html/

  db:
    image: postgres:9.6.2
    domainname: orcidhub.org.nz
    hostname: db
